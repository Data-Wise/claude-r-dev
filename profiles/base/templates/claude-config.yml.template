# Claude Code Configuration
# Generated by /project:init wizard
# Version: 1.2.0

package:
  name: "{{package_name}}"
  type: "{{package_type}}"
  domain: "{{package_domain}}"

notation:
  style: "{{notation_style}}"

object_system: "{{object_system}}"

function_categories:
  {{function_categories_yaml}}

inference:
  methods: [{{inference_methods_list}}]

testing:
  coverage_required: 80
  edge_cases:
    {{edge_cases_yaml}}

pkgdown:
  enabled: {{pkgdown_enabled}}
  color_scheme: "{{color_scheme}}"

code_style: "{{code_style}}"

dependencies:
  special: "{{special_dependencies}}"

metadata:
  created: "{{generated_date}}"
  generator: "claude-r-dev"
  version: "1.2.0"
